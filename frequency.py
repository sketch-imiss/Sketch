class Frequency:
    def __init__(self, dataset, filter_size, threshold, row, column, output):
        self.dataset = dataset
        self.filter_size = filter_size
        self.threshold = threshold
        self.row = row
        self.column = column
        self.output = output

    def run(self):
        print(123)